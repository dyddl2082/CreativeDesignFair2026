schema: macrobot.stored_object_runtime/v1
updated_at: "example"
profiles:
  Eraser:
    object_name: Eraser
    recorded_at: "example"
    recording_state: complete
    position_scope: pico_odom_session

    # Far pose where DINOv2 sees the complete object reliably.
    search_pose_odom:
      x_m: 0.0
      y_m: 0.0
      yaw_deg: 0.0
      reliable: true
      pico_time_ms: 100000
    recognition:
      point_base: {x: -0.48, y: 0.05, z: 0.09}
      score: 0.57
      minimum_range_m: 0.32
      maximum_range_m: 1.20

    # Static object point reconstructed in the same Pico odom session.
    object_point_odom:
      x: 0.48
      y: 0.05
      z: 0.09

    # Close pose recorded after teleop, within the arm's stable 30cm radius.
    grasp_pose_odom:
      x_m: 0.22
      y_m: 0.0
      yaw_deg: 0.0
      reliable: true
      pico_time_ms: 110000

    alignment:
      object_name: Eraser
      pick_profile: Eraser
      frame_id: base_link
      reference_point_base: {x: -0.26, y: 0.05, z: 0.09}
      recorded_at: "example"
      minimum_score: 0.0
      stability_count: 5
      stability_window_sec: 1.5
      stability_radius_m: 0.012
      bearing_tolerance_deg: 2.0
      range_tolerance_m: 0.015
      height_tolerance_m: 0.030
      max_turn_step_deg: 8.0
      max_move_step_m: 0.040
      turn_speed: 150
      move_speed: 80
      motion_timeout_sec: 10.0
      settle_sec: 0.45
      max_iterations: 20
      max_total_turn_deg: 120.0
      max_total_move_m: 0.60

    grasp:
      executor: keyframes
      keyframe_profile: Eraser
      pick_profile: Eraser

    search:
      max_yaw_deg: 35.0
      step_deg: 10.0
      dwell_sec: 1.2

    coarse_return:
      position_tolerance_m: 0.02
      angle_tolerance_deg: 3.0
      max_move_m: 1.5
      max_turn_deg: 180.0

    distance_handoff:
      enabled: true
      graspable_min_range_m: 0.08
      graspable_max_range_m: 0.30
      position_tolerance_m: 0.012
      angle_tolerance_deg: 2.0
      max_move_step_m: 0.10
      max_turn_step_deg: 15.0
      max_iterations: 16
      max_total_move_m: 0.80
      max_total_turn_deg: 180.0
      base_linear_error_fraction: 0.015
      base_turn_error_fraction: 0.020
      turn_translation_drift_m_per_360: 0.010
      maximum_uncertainty_m: 0.025
      maximum_object_relocation_m: 1.0
