# MacRobot aligned-depth proposal v1

Copy both package directories into `~/MacRobot/src/` on the Raspberry Pi:

- `macrobot_interfaces`
- `depth_candidate_proposal`

Then follow `depth_candidate_proposal/README.md`.
