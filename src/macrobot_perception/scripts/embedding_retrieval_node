#!/usr/bin/env bash
set -e

PYTHON_BIN=${MACROBOT_EMBEDDING_PYTHON:-"$HOME/MacRobot/.venv-embedding/bin/python"}
if [[ ! -x "$PYTHON_BIN" ]]; then
  PYTHON_BIN=$(command -v python3)
fi
exec "$PYTHON_BIN" -m embedding_retrieval.embedding_retrieval_node "$@"
